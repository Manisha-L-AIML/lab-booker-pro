// Deprecated stub kept only to avoid broken imports from older branches.
// Safe to delete this file entirely.
export function reportLovableError(
  _error?: unknown,
  _context?: Record<string, unknown>,
) {
  /* no-op */
}
